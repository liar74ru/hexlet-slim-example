<h1>User Details</h1>
<hr>
<p><strong>Name:</strong> <?= htmlspecialchars($user['name']) ?></p>
<p><strong>Email:</strong> <?= htmlspecialchars($user['email']) ?></p>

<style>
.user-actions {
    display: flex;
    gap: 10px;
    margin-top: 18px;
}
.user-actions form {
    margin: 0;
    display: inline;
}
.user-actions .btn {
    padding: 8px 18px;
    border: none;
    border-radius: 4px;
    background: #1976d2;
    color: #fff;
    font-size: 1em;
    cursor: pointer;
    transition: background 0.2s;
}
.user-actions .btn:hover {
    background: #125ea2;
}
.user-actions .btn-delete {
    background: #c62828;
}
.user-actions .btn-delete:hover {
    background: #a31515;
}

</style>

<div class="form-box user-actions">
    <form action="<?= $router->urlFor('users.index') ?>" method="get">
        <input type="submit" value="Back to Users" class="btn">
    </form>
  <form action="<?= $router->urlFor('users.edit', ['id' => $user['id']]) ?>" method="get">
    <input type="submit" value="Edit User" class="btn">
  </form>
  <form action="<?= $router->urlFor('users.delete', ['id' => $user['id']]) ?>" method="post">
    <input type="hidden" name="_METHOD" value="DELETE">
    <input type="submit" value="Delete User" class="btn btn-delete" onclick="return confirm('Are you sure you want to delete this user?');">
  </form>
</div>